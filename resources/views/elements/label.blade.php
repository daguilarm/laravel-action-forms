<div class="peer-disabled:opacity-50">
    <label 
        for="{{ $element }}" 
        id="label-{{ $uniqueKey }}"
        class="af_element_disabled_{{ $uniqueKey }} {{ $css->get('label') }}"
    >
        {{ $label }}
    </label>
</div>