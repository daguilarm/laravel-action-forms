<div class="peer-disabled:opacity-50">
    <label 
        for="{{ $element }}" 
        id="label-{{ $uniqueKey }}"
        class="{{ $css->get('label') }}"
    >
        {{ $label }}
    </label>
</div>